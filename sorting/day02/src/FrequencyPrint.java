public class FrequencyPrint {

    static String frequencyPrint(String s) {
        //split string at all spaces and put into list
        //for each word in list, check to see if word is in hash map, if it isnt -add it
        //if it is incrememnt its count by one
        //for each element in hash map, check value, and print key that number of times
         return null;}

}
